rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Windows kurulum dosyası — herkese okuma, yazma yalnızca konsol/CI
    match /downloads/{fileName} {
      allow read: if true;
      allow write: if false;
    }
  }
}
